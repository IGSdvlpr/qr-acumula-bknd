{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.js" },
    { "src": "/tarjeta/(.*)", "dest": "/server.js" },
    { "src": "/public/(.*)", "dest": "/public/$1" }
  ]
}